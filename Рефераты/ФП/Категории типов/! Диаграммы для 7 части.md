
# Объект категории запятой
```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath
%\mathversion{bold}

\begin{tikzcd}[
  scale cd=2,
  sep=10em,
  column sep=20em,
  /tikz/execute at end picture={
	\begin{scope}[on background layer]
		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Blue!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  circle,              % Форма
		  minimum size=3.6cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(a),             % Охватываем узел (a)
		  label={[font=\huge]140:$\mathcal{A}$}      % Подпись СНАРУЖИ сверху
		] (ca) {};

		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Green!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  circle,              % Форма
		  minimum size=3.6cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(b),             % Охватываем узел (a)
		  label={[font=\huge]140:$\mathcal{B}$}      % Подпись СНАРУЖИ сверху
		] (cb) {};

		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Red!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  ellipse,              % Форма
		  minimum height=10cm,
		  minimum width=10cm,
		  fit=(Fa) (Gb),
		  label={[font=\huge]140:$\mathcal{C}$}      % Подпись СНАРУЖИ сверху
		] (cFaGb) {};

		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Blue!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  circle,              % Форма
		  minimum size=3.6cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(Fa),             % Охватываем узел (a)
		  label={[font=\huge]30:$F\mathcal{A}$}      % Подпись СНАРУЖИ сверху
		] (cFa) {};

		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Green!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  circle,              % Форма
		  minimum size=3.6cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(Gb),             % Охватываем узел (a)
		  label={[font=\huge]30:$G\mathcal{B}$}      % Подпись СНАРУЖИ сверху
		] (cGb) {};
	\end{scope}
  }
]

|[alias=a]| a
\ar[r, "F"{pos=0.3}, Blue, double distance=2]
&
|[alias=Fa]| Fa
\ar[d, Red, "{f:\mathrm{Hom}_{\mathcal{C}}(Fa, Gb)}"']

\\

|[alias=b]| b
\ar[r, "G"{pos=0.3}, Green, double distance=2]
&
|[alias=Gb]| Gb

\end{tikzcd}
\end{document}
```

# Морфизм категории запятой
Квадрат справа должен коммутировать.
```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath

\begin{tikzcd}[
  scale cd=2,
  sep=10em,
  /tikz/execute at end picture={
	\begin{scope}[on background layer]
		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Blue!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  ellipse,              % Форма		  
		  fit=(a) (aa),
		  label={[font=\huge]170:$\mathcal{A}$}      % Подпись СНАРУЖИ сверху
		] (caa) {};
		
		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Green!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  ellipse,              % Форма		  
		  fit=(b) (bb),
		  label={[font=\huge]170:$\mathcal{B}$}      % Подпись СНАРУЖИ сверху
		] (cbb) {};
		
		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Red!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  circle,              % Форма		  
		  fit=(Fa) (Gbb),
		  label={[font=\huge]140:$\mathcal{C}$}      % Подпись СНАРУЖИ сверху
		] (cbb) {};
	\end{scope}
  }
]

|[alias=a]| a
\ar[r, "h", Blue]
&
|[alias=aa]| a'
&
|[alias=Fa]| Fa
\ar[r, "Fh", Blue]
\ar[d, "f", Red]
&
|[alias=Faa]| Fa'
\ar[d, "f{\displaystyle '}", Red]
\\

|[alias=b]| b
\ar[r, "g", Green]
&
|[alias=bb]| b'
&
|[alias=Gb]| Gb
\ar[r, "Gg", Green]
&
|[alias=Gbb]| Gb'

\end{tikzcd}
\end{document}
```


# Диестественное преобразование

```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath

\begin{tikzcd}[
  scale cd=2,
  sep=5em,
  /tikz/execute at end picture={
  }
]

&
P(b,b)
\ar[r, "{\hat\alpha_b}", Red]
&
Q(b,b)
\ar[rd, "{Q(id,h)}", Blue]
&

\\


P(a,b)
\ar[ur, "{P(h,id)}", Green]
\ar[rd, "{P(id,h)}"', Green]
&
&
&
Q(b,a)

\\

&
P(a,a)
\ar[r, "{\hat\alpha_a}", Red]
&
Q(a,a)
\ar[ur, "{Q(h,id)}"', Blue]
&

\end{tikzcd}
\end{document}
```

# Естественное преобразование

```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath

\begin{tikzcd}[
  scale cd=2,
  sep=5em,
  /tikz/execute at end picture={
  }
]

&
P(b,b)
\ar[rr, "{\alpha_{bb}}", Red]
&
&
Q(b,b)
\ar[rd, "{Q(id,h)}", Blue]
&

\\


P(a,b)
\ar[ur, "{P(h,id)}", Green]
\ar[rd, "{P(id,h)}"', Green]
\ar[rr, "{\alpha_{ab}}", Red]
&
&
Q(a,b)
\ar[ur, "{Q(h,id)}"', Blue]
\ar[rd, "{Q(id,h)}", Blue]
&
&
Q(b,a)

\\

&
P(a,a)
\ar[rr, "{\alpha_{aa}}", Red]
&
&
Q(a,a)
\ar[ur, "{Q(h,id)}"', Blue]
&

\end{tikzcd}
\end{document}
```


# Универсальное свойство коконца


```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath

\begin{tikzcd}[
  scale cd=2,
  sep=10em,
  /tikz/execute at end picture={
  }
]

|[Green]| 
\mathrm{Coend}\,P
\ar[r, "{u}", Red]
&
|[blue]| x

\\

P(a,a)
\ar[u, "{i_a}", Green]
\ar[ur, "{\alpha_a}", Blue]
&


\end{tikzcd}
\end{document}
```


# Универсальное свойство конца


```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath

\begin{tikzcd}[
  scale cd=2,
  sep=10em,
  /tikz/execute at end picture={
  }
]

|[Green]| 
\mathrm{End}\,P
\ar[d, "{\pi_a}", Green]
&
|[blue]| x
\ar[l, "{u}", Red]
\ar[dl, "{\alpha_a}", Blue]

\\

P(a,a)
&


\end{tikzcd}
\end{document}
```



# Функторы категории запятой
```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath
%\mathversion{bold}

\begin{tikzcd}[
  scale cd=2,
  row sep=10em,
  column sep=2em,
  /tikz/execute at end picture={
	\begin{scope}[on background layer]
 		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Blue!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  circle,              % Форма
		  minimum size=3.6cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(a),             % Охватываем узел (a)
		  label={[font=\huge]140:$\mathcal{A}$}      % Подпись СНАРУЖИ сверху
		] (ca) {};

 		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Green!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  circle,              % Форма
		  minimum size=3.6cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(b),             % Охватываем узел (a)
		  label={[font=\huge]40:$\mathcal{B}$}      % Подпись СНАРУЖИ сверху
		] (ca) {};

 		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Black!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  ellipse,              % Форма
		  minimum size=3cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(abf),             % Охватываем узел (a)
		  label={[font=\huge]180:$F \downarrow G$}      % Подпись СНАРУЖИ сверху
		] (ca) {};

 		\node[
		  draw=black!80,       % Обводка (как в filldraw)
		  fill=Red!10,        % Заливка (как в filldraw)
		  very thick,          % Толщина линии
		  ellipse,              % Форма
		  minimum size=3cm,  % Диаметр (2 * 1.8 из вашего примера)
		  fit=(Fa) (Gb),             % Охватываем узел (a)
		  label={[font=\huge]180:$\mathcal{C}$}      % Подпись СНАРУЖИ сверху
		] (ca) {};

 	\end{scope}
  }
]

&
|[alias=Fa]| Fa
\ar[rr, Red, bend left=10, "{\eta_{abf\hspace{-2mm}}}=f", line width=3]
\ar[rr, Red, line width=1]
\ar[rr, Red, bend right=10, line width=1]
&
&
|[alias=Gb]| Gb
&

\\


|[alias=a]| a
\ar[ru, "F", Blue, double distance=2]
&
&
&
&
|[alias=b]| b
\ar[lu, "G"', Green, double distance=2]

\\

&&
|[alias=abf]| (a,b,f)
\ar[llu, "{\Pi_{\mathcal{A}}}", double distance=2]
\ar[rru, "{\Pi_{\mathcal{B}}}"', double distance=2]

\ar[luu, LightBlue, "{F \circ \Pi_{\mathcal{A}}}"'{pos=0.6, name=FP, description}, double distance=2, bend left=10]
\ar[ruu, LightGreen, "{G \circ \Pi_{\mathcal{B}}}"{pos=0.6, name=GP, description}, double distance=2, bend right=10]
\ar[from=FP, to=GP, Red, "{\eta}", squiggly]
&&

\end{tikzcd}
\end{document}
```


# Композиция профункторов


```tikz
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{xcolor}
\usetikzlibrary{backgrounds, fit}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes.geometric}
\tikzcdset{scale cd/.style=
  {every
    label/.append style={scale=#1, transform shape},
    cells={nodes={scale=#1, transform shape}},
    arrows={ultra thick, -{Stealth[scale=1]}},
  },
}
\begin{document}

\boldmath

\begin{tikzcd}[
  scale cd=2,
  sep=5em,
  row sep=15em,
  /tikz/execute at end picture={
  }
]

\mathcal{A}
\ar[rr, "P", Blue, bend left=10, double distance=2]
\ar[rrrr, "{Q \circ P}", Red, bend left, double distance=2]

\ar[rrd, "{\mathcal{A}^{op} \times \mathcal{B} \to \mathcal{V}}"'{pos=0.2}, Blue, bend right=40]
\ar[rrd, "{\mathcal{A}^{op} \times \mathcal{C} \to \mathcal{V}}"{pos=0.45}, Red, bend left=40]
&&
\mathcal{B}
\ar[rr, "Q", bend left=10, Green, double distance=2]

\ar[d, "", Blue, bend right=80]
\ar[d, "", Green, bend left=80]
&&
\mathcal{C}
\ar[lld, "", "{\mathcal{B}^{op} \times \mathcal{C} \to \mathcal{V}}"{pos=0.2}, Green, bend left=40]
\ar[lld, "", Red, bend right=40]

\\

&
|[alias=Va]| 
&
\mathcal{V}
&
|[alias=Vc]|
&


\end{tikzcd}
\end{document}
```
