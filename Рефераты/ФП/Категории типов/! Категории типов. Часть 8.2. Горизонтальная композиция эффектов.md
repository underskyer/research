
# Композиции контейнеров

Когда мы говорим о композиции эффектов, то, как правило, имеется в виду вычисления в контейнере, являющимся композицией из нескольких контейнеров эффектов. В [предыдущей части](https://habr.com/ru/articles/1055520/) мы говорили о вертикальной композиции, основанном на последовательном вызове конструкторов типов как функций уровня типов `(F ∘ G)[A] = F[G[A]]`. Но в более общем случае под композицией понимается просто бинарная операция ([магма](https://ru.wikipedia.org/wiki/%D0%9C%D0%B0%D0%B3%D0%BC%D0%B0_(%D0%B0%D0%BB%D0%B3%D0%B5%D0%B1%D1%80%D0%B0))) на конструкторах типов, и помимо их функциональной (вертикальной) композиции мы можем также использовать любые алгебраические выражения, составленных из базовых операций:
```scala
infix type ==>[F[_], G[_]] = [A] =>> F[A] => G[A]  
infix type :×:[F[_], G[_]] = [A] =>> F[A] ×  G[A]
infix type :+:[F[_], G[_]] = [A] =>> F[A] +  G[A] // cats.data.EitherK[F, G, A]
```

Контейнер `F ==> G` получается инвариантным и, не будучи эндофунктором, не годится в роли способа композиции монад. Для произведения монад монаду реализовать можно:
```scala
// для удобства подключим синтаксис из библиотки Cats
import cats.instances.tuple._  // предоставляет экземпляр Bifunctor для Tuple2
import cats.syntax.arrow._     // даёт оператор *** (split)
import cats.syntax.bifunctor._ // добавляет синтаксический метод .bimap

given prodFunctor: [F[_]: Functor as F, G[_]: Functor as G] => Functor[F :×: G] =  
  [A, B] => (f: A => B) => F(f) *** G(f) // (faga: F[A] × G[A]) => faga.bimap(fFunctor(f), gFunctor(f))  
  
given prodMonad: [F[_]: Monad as F, G[_]: Monad as G] => Monad[F :×: G] = (  
  fmap = prodFunctor,  
  pure = [A] => (a: A) => F.pure(a) -> G.pure(a),  
  flatten = [A] => (fgfga: (F :×: G)[(F :×: G)[A]]) =>  
    fgfga.bimap(_.flatMap(_._1), _.flatMap(_._2))  
)
```
Вычисления в контейнерах-множителях производятся *независимо друг от друга*. Например, если мы опишем монаду для константного функтора `Const[String][_]`, то композиция `Const[String] :×: IO` будет эквивалентна вертикальной композиции `Writer[String] ∘ IO`, что позволяет вести журнал *параллельно* операциям ввода-вывода. Но обычно редко возникает необходимость дублировать все вычисления с несколькими эффектами.

Для суммы контейнеров снова не получится собрать честную монаду, если контейнеры неизоморфны. Но именно такая композиция делает свободную монаду полезной для практического использования.


# Свободные монады

Устройству свободных монад посвящена одна из предыдущих частей обзора. [Одна](https://habr.com/ru/articles/1044512/#universal) из представленных там реализаций `FreeU[F[_]]` опирается на универсальное свойство — возможность получить экземпляр монады `M[_]`, если предоставлен интерпретатор функтора в эту монаду:
```scala
type FreeU[F[_]] = [X] =>> [M[_]] => Monad[M] ?=> (F ~> M) => M[X]
```

Если подставить вместо `F` сумму контейнеров `G :+: H`, то суммарный интерпретатор распадается на произведение интерпретаторов слагаемых:
$$
(G + H) \rightsquigarrow M \cong
(G \rightsquigarrow M) \times (H \rightsquigarrow M).
$$
Интерпретация контейнера в монаду как раз и наделяет его эффектом. Значит, если нам нужны эффекты двух контейнеров, мы просто оборачиваем их в свободную монаду и предоставляем независимые интерпретаторы этих эффектов!

Подробнее технику программирования на свободных монадах я уже описывал [тут](https://habr.com/ru/articles/807495/#free). Подсвечу основные моменты:
- сперва собирается горизонтальная композиция контейнеров необходимых эффектов `Total = F :+: G :+: H :+: ...`,
- затем конструкторы этих контейнеров (декларации эффектов) «поднимаются в мир» свободного контейнера `Free[Total]` (благодаря универсальному свойству суммы типов такое естественное преобразование `F ~> Free[Total]` существует всегда и реализуется в библиотеках, например, `Free.liftInject` в Cats),
- из этих поднятых конструкторов строится свободная программа типа `Free[Total][A]`, задействующая монадную композицию,
- из интерпретаторов отдельных эффектов `F ~> M` собирается (тоже алгебраически) тотальный интерпретатор `Total ~> M`,
- в конце свободная программа интерпретируется тотальным интерпретатором.

В этом и заключается основная идея *горизонтальной композиции эффектов*. По очевидным причинам такой подход также называют **алгебраическими эффектами**, но важно иметь в виду, что оба эти термина не обязательно относятся к композиции монад.

## Eff

Техника свободных программ обязывает заранее фиксировать перечень доступных эффектов в определении контейнера «комбинированного эффекта» `Total[_]`. Это оказывается не очень удобным — при добавлении к программе нового эффекта придётся изменять и тотальный контейнер. Поэтому привлекательной становится идея *расширяемых эффектов*, позволяющая программистам абстрагироваться от фиксированного стека эффектов, сосредоточившись только на необходимых для каждого шага.

Тотальный контейнер играет роль метки для компилятора, обеспечивающего статическую проверку связи описания эффектов и их интерпретации. Нарочно создавать экземпляры этого контейнера не нужно, так что по сути он является *фантомным*. Библиотека [Eff](https://github.com/atnos-org/eff) предлагает использовать вместо обобщённого типа контейнера простой фантомный тип-токен `Fxn[F, G, ...]`:
```scala
Free[F :+: G :+: …, A] ≅ Eff[Fxn[F, G, ...], A]
```

Смысл токена заключатся в том, что с его помощью неявно вытягиваются ключевые возможности суммарного контейнера: инъекции `F ~> Total` и проекции (с проверкой) `Total ~> Option ∘ F`. Они оборачиваются к класс типов примерно такого вида:
```scala
type MemberIn[F[_]] = [T[_]] =>> (
  inject:  F ~> T,
  project: T ~> (Option ∘ F)
)
```

Библиотека Eff предоставляет макросы автоматической генерации экземпляров этого класса типов с канонической реализацией. Это позволяет полностью абстрагироваться от токена, переложив доступ к эффектам на неявные значения класса типов. Вот пример из официальной документации:
```scala
// Пользовательские контейнеры эффектов 
trait Interact[A]
trait DataOp  [A]

// классы типов эффектов
type _interact   [R] = Interact       |= R
type _dataOp     [R] = DataOp         |= R
type _promtReader[R] = Reader[String] |= R

def program[R: {_promtReader, _interact, _dataOp}]: Eff[R, String] =
for {
  cat   <- askUser("What's the kitty's name?")   // использует эффект Interact
  _     <- addCat(cat)                           // использует эффект DataOp
  cats  <- getAllCats                            // использует эффект DataOp
  promt <- ask                                   // использует эффект Reader[String]
  _     <- tellUser(promt + cats.mkString(", ")) // использует эффект Interact
} yield "Ok"
```

После ~~несколько замороченного~~ описания интерпретаторов пользовательских эффектов (`runInteract`, `runDataOps`), исполнение свободной программы выглядит так:
```scala
val result: String = program
  .runReader("All cat's names: ") // интерпретация эффекта Reader[String]
  .runInteract                    // интерпретация эффекта Interact
  .runDataOps                     // интерпретация эффекта DataOp
  .run                            // финальная интерпретация в Id
```
Обратите внимание, эффекты можно интерпретировать *по очереди в произвольном порядке*, причём зачастую можно выбрать, исключить ли упоминание эффекта из типа-токена, или же оставить его в стеке (например, обработка исключений может пригодится и до, и после интерпретаций других эффектов).

Другие примеры программ с расширяемыми эффектами на базе контейнера Eff можно найти на страницах [официальной документации](https://atnos-org.github.io/eff/org.atnos.site.Tutorial.html) библиотеки.


## Turbolift

Библиотека [Turbolift](https://marcinzh.github.io/turbolift/) 



# MTL & Tagless Final

**Подход на основе тайпклассов (Cats MTL, final tagless)**  
Вместо конкретной монады-трансформера код пишется против абстрактных возможностей (`MonadState`, `MonadError`, `MonadThrow`). Разные реализации могут удовлетворять этим тайпклассам, а композиция свойств происходит на уровне ограничений типа — это горизонтальное смешивание поведения без фиксации структуры стека.

MTL
https://typelevel.org/cats-mtl/getting-started.html




# Direct style (прямой стиль) — исчезновение монад при сохранении горизонтальной природы

**Что это:**  
Стиль написания кода, который выглядит как обычный императивный, но под капотом использует приостановку (continuations, виртуальные потоки), а не явные `flatMap`-цепочки. В Scala это представлено библиотеками **Ox**, **Gears** и самим **Kyo** (в режиме `direct`).

**Как вписывается в схему:**

- Прямой стиль **не меняет композиционный принцип, а скрывает его**. Эффекты по-прежнему компонуются горизонтально, но вместо `for`-компренхеншена или `>` вы просто вызываете функции, которые могут приостанавливаться, бросать ошибки или читать окружение.
    
- Это можно рассматривать как **синтаксический слой** над горизонтальной композицией. Вместо явного конструирования сложных типов (как в пункте 6 — final tagless) вы используете capabilities (возможности) как неявные параметры, и компилятор сам отслеживает, какие эффекты нужны.
    
- Например, в Kyo direct одна и та же композиция `Abort[String] > Async > Var[Int]` выглядит как императивная функция с тремя параметрами-возможностями, что является эволюцией пункта 6 (тайпклассы) в сторону ещё более естественного кода.
    

---
[Direct-style Effects Explained](https://noelwelsh.com/posts/direct-style/)


## Ox

https://atnos-org.github.io/eff/org.atnos.site.Tutorial.html

## Kyo

https://koka-lang.github.io/koka/doc/book.html#sec-effect-types

## Capabilities

(отслеживаемые возможности) — горизонтальная композиция на уровне типов без трансформеров

**Что это:**  
Подход, при котором каждое «умение» программы (кидать ошибки, читать конфигурацию, мутировать состояние) представляется отдельным параметром, обычно передаваемым через `given`/`using` или в виде отдельного типа-эффекта. В отличие от тайпклассов, capabilities часто более гранулярны и не требуют глобального разрешения.

**Как вписывается в схему:**

- Это прямое развитие **пункта 6 (final tagless)**, но без привязки к монадическому стеку. Функция требует одновременно `CanFail[E]`, `CanRead[Config]` и `CanState[Int]` — это и есть горизонтальная комбинация независимых свойств, заданная на уровне типов.
    
- Capabilities можно компоновать с помощью пересечения типов (`&`) или объединения (`>` в Kyo), что даёт **горизонтальную семантику без вертикальных слоёв**.
    
- В экосистеме Scala такой подход активно используется в библиотеке **Caprese**, а также встроен в **Kyo** (где возможности представлены типами эффектов). Более того, грядущий **Project Caprese** для Scala 3 делает возможности первоклассными конструкциями языка, позволяя описывать горизонтальную композицию эффектов напрямую в сигнатурах функций.

# Промежуточный итог

### Алгебраические эффекты — теоретический фундамент для 

**Что это:**  
Подход, при котором эффекты описываются как набор операций (алгебра), а вся программа — как дерево этих операций. Композиция эффектов достигается объединением алгебр, а не вложением монад.

**Связь с горизонтальной композицией:**

- Алгебраические эффекты **естественно обобщают** пункты 2 (Free + Coproduct) и 4 (библиотека `eff`). Вместо фиксированной структуры вложенности все эффекты лежат «на одном уровне» в виде копродукта (суммы) алгебр, то есть чисто горизонтально.
    
- Они позволяют **добавлять новые эффекты в программу без изменения существующего кода** — именно это свойство называется открытой (open) композицией и считается высшим проявлением горизонтальности.
    
- Библиотека `eff` из пункта 4 — это ранняя реализация алгебраических эффектов в Scala. Современные библиотеки (вроде Kyo) используют тот же принцип, но идут дальше в эргономике.
    
