
Название «плотность» (density) в теории категорий — это не просто красивое слово, оно заимствовано из топологии и анализа, но переведено на язык универсальных свойств.

Чтобы понять, почему монада Ran_F F называется монадой коплотности, а комонада Lan_F F — комонадой плотности, нужно разобрать понятие плотного функтора.

—

▌1. Интуиция: что такое «плотность»?

В топологии множество A плотно в X (как рациональные числа в вещественных), если любая точка X находится «бесконечно близко» к точкам из A. То есть любую точку x ∈ X можно аппроксимировать последовательностью из A.

В теории категорий идея та же: функтор F: (𝒞)→(𝒟) называется плотным, если любой объект категории (𝒟) можно «собрать» (аппроксимировать) из образов объектов категории (𝒞).

•  На языке формул: Любой объект D ∈(𝒟) является копределом «диаграммы всех объектов из (𝒞), отображающихся в D».
•  Если F плотный, то каждый D — это канонический копредел:
  $D ≅ ∫^{C ∈ 𝒞} (F(C), D) · F(C)$
  (Это в точности формула для Lan_F F!)

▌2. Плотность и Комонада Плотности (Lan_F F)

Комонада плотности G = Lan_F F берет объект D и пытается «пересобрать» его, используя только информацию, доступную через функтор F.

•  Если F — плотный функтор, то Lan_F F ≅ Id. Это значит, что аппроксимация идеальна, потерь данных нет.
•  Если F не плотный, то Lan_F F(D) — это «лучшая попытка» воссоздать D, используя только образы функтора F.
•  Почему «плотность»? Потому что эта конструкция измеряет, насколько «плотно» образы (𝒞) заполняют категорию (𝒟). Если они лежат плотно, мы можем восстановить любой объект.

Пример: Вложение Йонеды Y: (𝒞)→ [ (𝒞)^(op), Set ] является плотным функтором. Любой препучок — это копредел представимых функторов. Это фундаментальная «теорема о плотности» в теории категорий.

—

▌3. Коплотность и Монада Коплотности (Ran_F F)

Теперь перейдем к вашему основному интересу. Монада коплотности — это дуальная концепция.

•  Если плотность связана с копределами (сборка объекта «из кирпичиков»), то коплотность связана с пределами (определение объекта через «наблюдения» или «коограничения»).
•  Формула монады коплотности:
  (Ran_F F)(D) ≅∫_(C ∈(𝒞)) [(𝒟)(D, F(C)), F(C)]
  Здесь мы не «собираем» D, а смотрим на все способы отобразить D в объекты «мира F» и берем предел этих отображений.

Почему «КО-плотность»?
1. Формально: Потому что она использует Ran (правое расширение), которое дуально Lan. В категории [(𝒟), (𝒟)] копределы заменяются пределами.
2. Интуитивно: Плотность говорит: «Могу ли я собрать этот объект из этих частей?». Коплотность говорит: «Могу ли я полностью описать этот объект через его отношения с другими (внешними) объектами?».

Пример из программирования (Haskell/Scala):
Монада коплотности для функтора f это Codensity f a = forall b. (a -> f b) -> f b.
Это описание типа a через «все возможные продолжения», которые уходят в контекст f. Это очень похоже на теорему Йонеды, но «с другой стороны». Мы не знаем, что такое a, но мы знаем всё, что с ним можно сделать внутри f.

—

▌4. Итог: Почему именно эти названия?

Резюме для вашего контекста:
Когда вы берете $(Lan_F Id) \circ F$, вы строите монаду на "малой" категории $\mathcal{C}$, используя "большую" категорию $\mathcal{D}$ как пространство для аппроксимации. Это тоже своего рода "плотность": вы смотрите, как объекты $\mathcal{C}$ "плотно" или "не очень" сидят внутри структуры, которую навязывает им $\mathcal{D}$ через расширение Кана.

Это название подчеркивает, что мы не просто манипулируем функторами, а пытаемся восстановить или расширить идентичность объектов, основываясь на их положении относительно друг друга


Чтобы понять «плотность» не на словах, а на формулах, нужно перекинуть мостик от топологии (где это слово родилось) к теории категорий через понятие аппроксимации.

▌1. Что такое плотность в формулах?

В топологии A плотно в D, если любая точка D — это предел последовательности из A.
В категориях функтор F: (𝒞)→(𝒟) плотный, если любой объект D ∈(𝒟) — это канонический копредел объектов из (𝒞).

Формула плотности:
Lan_F F ≅ Id_(𝒟)

Как это читать:
Lan_F F(D) — это «наилучшая попытка собрать объект D, используя только кирпичики из образа функтора F».
•  Если F плотный, то «попытка» идеальна: мы получаем сам D.
•  Если F не плотный, то Lan_F F(D) — это «суррогат» объекта D, собранный из того, что было доступно в (𝒞).

—

▌2. Примеры: Плотные vs Неплотные

▍Пример А: Базис в векторных пространствах (Плотный)
Пусть (𝒟) = Vect_k (категория конечномерных векторных пространств).
Пусть (𝒞) — категория с одним объектом ∙, а функтор F: (𝒞)→(𝒟) отправляет этот объект в одномерное пространство (скалярное поле k).

•  Вопрос: Можем ли мы собрать любое векторное пространство V, используя только k?
•  Ответ: Да, V ≅⊕_(i=1)^n k. Прямая сумма — это копредел. Любой вектор — это линейная комбинация «базисных» элементов из k.
•  Результат: Этот функтор плотный. Любое пространство «плотно» окружено своими одномерными подпространствами. Lan_F F(V) ≅ V.

▍Пример Б: Четные множества (Неплотный)
Пусть (𝒟) = Set (все множества).
Пусть (𝒞) — подкатегория, состоящая только из множеств с четным количеством элементов {(0, 2, 4, …, и F — вложение.

•  Вопрос: Можем ли мы собрать множество из 3 элементов {(a, b, c, используя только «четные» множества?
•  Интуиция: Мы можем взять {(a, b и {(c, d, но как избавиться от лишнего d? Копределы в Set работают через склейки и объединения. Мы не сможем в точности «вырезать» нечетность, используя только четные кирпичики без потерь.
•  Результат: Этот функтор не плотный. Lan_F F({(a, b, c) даст нам что-то другое (скорее всего, большее по размеру множество), потому что «разрешающей способности» четных множеств не хватает, чтобы точно описать нечетное.

—

▌3. Что значит «как объекты (𝒞) плотно сидят в (𝒟)»?

Представьте, что категория (𝒟) — это высококачественная фотография (высокое разрешение), а категория (𝒞) — это набор крупных пикселей (низкое разрешение). Функтор F проецирует пиксели на фото.

Когда мы берем Lan_F F(D), мы делаем следующее:
1. Смотрим на объект D (оригинал фото).
2. Пытаемся представить его как комбинацию пикселей из (𝒞).
3. Результат Lan_F F(D) — это пикселизированная версия оригинала.

Теперь интерпретируем фразу:
•  Если «объекты (𝒞) сидят плотно», значит, наши пиксели настолько мелкие (или их так много), что пикселизированная версия неотличима от оригинала (Lan_F F ≅ Id).
•  Если они сидят «не очень плотно», то после прохода через Lan_F F мы увидим «размытие» или «искажение».

Как на это «посмотреть»?
Нужно посмотреть на единицу и коединицу расширения Кана:
•  Если Id → Lan_F F — это изоморфизм, значит, потерь нет.
•  Если это не изоморфизм, то «разница» между Id(D) и Lan_F F(D) — это и есть мера того, чего не хватает категории (𝒞), чтобы полностью покрыть (𝒟).

—

▌4. Коплотность: взгляд «снаружи»

Монада коплотности Ran_F F делает то же самое, но через наблюдения (стрелки из объекта).

•  Плотность (Lan): «Могу ли я собрать этот объект из запчастей (𝒞)?» (Конструктор).
•  Коплотность (Ran): «Могу ли я отличить этот объект от других, используя только датчики из (𝒞)?» (Тестирование/Наблюдение).

Пример коплотности:
Если (𝒟) — категория всех групп, а (𝒞) — только циклическая группа (ℤ).
Если вы можете полностью описать любую группу G, просто зная все способы отобразить (ℤ)→ G (что эквивалентно знанию всех элементов группы), то (ℤ) «коплотно» в категории групп.

▌Резюме:
«Посмотреть на плотность» — значит проверить, насколько сильно портится объект, если его сначала «разобрать» до уровня объектов (𝒞), а потом «собрать» обратно в (𝒟).
•  Нет порчи = плотный функтор.
•  Сильная порча = разреженная подкатегория.