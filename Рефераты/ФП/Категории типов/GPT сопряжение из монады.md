# Сопряжение из монады
В теории категорий есть фундаментальный факт: любая монада $T$ порождается каким-либо сопряжением. 
Обычно это доказывается через:
•   Категорию Клейсли (самое экономное сопряжение).
•   Категорию Эйленберга-Мура (самое «богатое» сопряжение).

Разгадка кроется в том, что для монады $T = Ran_F F$ это сопряжение не обязано вовлекать исходный функтор $F$ и исходную категорию $\mathcal{C}$.

▌1. Два разных сопряжения

Пусть $F: \mathcal{C} \to \mathcal{D}$. Мы построили монаду $T = Ran_F F$ на категории $\mathcal{D}$.

1. Сопряжение, которого может не быть: $L \dashv F$, где $L: \mathcal{D} \to \mathcal{C}$.
  Вы верно говорите: мы исходим из того, что такого $L$ нет.
2. Сопряжение, которое есть всегда: Для любой монады $T$ на $\mathcal{D}$ мы можем построить категорию алгебр Эйленберга-Мура $\mathcal{D}^T$. Там всегда есть каноническое сопряжение $F^T \dashv U^T$:
  •  $U^T: \mathcal{D}^T \to \mathcal{D}$ (забывающий функтор).
  •  $F^T: \mathcal{D} \to \mathcal{D}^T$ (свободный функтор).
  •  И по определению $T = U^T \circ F^T$.

В чем фокус?
Функтор $U^T$ (забывающий функтор из категории алгебр) — это не то же самое, что ваш исходный функтор $F$. И категория алгебр $\mathcal{D}^T$ — это не то же самое, что ваша исходная категория $\mathcal{C}$.

▌2. Что же такое «виртуальная монада»?

Под словом «виртуальная» я имел в виду вот что:

Если бы у $F$ был левый сопряженный $L$, то монада $T$ была бы просто $F \circ L$. В этом случае $F$ сам бы играл роль «забывающего функтора» в сопряжении, порождающем монаду.

Но когда у $F$ нет левого сопряженного, $Ran_F F$ все равно создает монаду. Эта монада «выглядит и пахнет» как монада сопряжения, но она не может найти своего «родителя» $L$ в категории $\mathcal{C}$. Она находит его в другой категории — в категории своих собственных алгебр.

Связь между ними:
Существует канонический функтор $K: \mathcal{C} \to \mathcal{D}^T$ (функтор сравнения). Он связывает ваш исходный функтор $F$ и забывающий функтор монады коплотности $U^T$:
$$F \cong U^T \circ K$$
То есть $F$ — это всего лишь «часть» забывающего функтора $U^T$.

▌3. Интуитивный пример (программирование)

Представьте монаду Codensity f.
•  Функтор f — это просто какой-то тип данных (например, BinaryTree), у которого нет flatMap и который не является результатом никакого сопряжения. У него «нет пары».
•  Но Codensity BinaryTree — это монада. Значит, для неё есть сопряжение. Где оно?
•  Оно существует между категорией всех типов и категорией «алгебр» этой конкретной монады коплотности.
•  Мы как бы «симулировали» существование сопряжения для BinaryTree, создав вокруг него более сложную структуру.

▌Итог
Утверждение Милевски «Каждая монада является коплотной монадой для этого сопряжения» означает следующее:

1. Возьмите монаду $T$.
2. Найдите её каноническое сопряжение $F^T \dashv U^T$ (через алгебры).
3. Тогда $T \cong Ran_{U^T} U^T$.

А ваше замечание говорит о другом: «Мы можем построить коплотную монаду даже для функтора $F$, который не является частью никакого сопряжения».

Это правда. Но как только вы её построили, вы создали новое сопряжение (с категорией алгебр), в котором $F$ будет участвовать лишь опосредованно.

Почему это важно? Это позволяет нам работать с любым функтором $F$ так, будто у него есть сопряженный, даже если в реальности (в категории $\mathcal{C}$) его не существует. Мы как бы «выдумываем» недостающую структуру. Именно в этом смысле она «виртуальна» относительно исходной категории $\mathcal{C}$.


# Функтор сравнения
Функтор сравнения K — это своего рода «мост», который показывает, как исходный функтор F соотносится с алгебраической структурой, которую он сам же и породил.

Если T = Ran_F F — монада коплотности, то функтор сравнения K: (𝒞)→(𝒟)^T строится удивительно естественным образом.

▌1. Что нам нужно для построения?
Напомним компоненты:
1. F: (𝒞)→(𝒟).
2. T = Ran_F F (монада на (𝒟)).
3. β: T ∘ F → F — универсальное преобразование (коединица) из определения расширения Кана.
4. (𝒟)^T — категория алгебр Эйленберга-Мура. Её объекты — это пары (D, θ), где D ∈(𝒟), а θ: TD → D — структура алгебры.

▌2. Определение функтора K на объектах
Для каждого объекта C ∈(𝒞) нам нужно получить объект в (𝒟)^T.
Объект категории алгебр — это носитель и структура.

•  Носитель: Мы просто берем образ объекта под действием F. То есть D = F(C).
•  Структура алгебры: Нам нужно отображение T(F(C)) → F(C). Но у нас как раз есть универсальное преобразование β! Его компонент на объекте C имеет ровно этот тип:
  β_C: T(F(C)) → F(C)

Итак, K(C) = (F(C), β_C).

▌3. Почему это действительно алгебра?
Чтобы (F(C), β_C) был корректной алгеброй монады, должны выполняться два условия (единица и умножение монады).
•  Универсальное свойство Ran_F F как раз гарантирует, что β ведет себя согласованно с единицей η и умножением μ монады коплотности. 
•  Фактически, монада коплотности определяется так, чтобы β была «каноническим действием» монады на образах функтора F.

▌4. Определение K на морфизмах
Для морфизма f: C → C' в категории (𝒞) нам нужно получить морфизм алгебр:
K(f): (F(C), β_C) → (F(C'), β_(C'))
Морфизм алгебр — это просто морфизм в базовой категории (𝒟), который «дружит» со структурой алгебры.
•  Мы берем K(f) = F(f).
•  Условие «дружбы» (коммутативность квадрата) выглядит так:
  β_(C')· T(F(f)) = F(f) ·β_C
•  Это равенство выполняется автоматически, потому что β: T ∘ F → F — это естественное преобразование. А естественность как раз и означает коммутативность такого квадрата для любого f.

▌5. Что это дает нам в итоге?

Теперь мы видим полную картину «факторизации» функтора F:
1. У нас есть забывающий функтор U^T: (𝒟)^T →(𝒟), который просто отбрасывает структуру алгебры: U^T(D, θ) = D.
2. Заметим, что если мы сначала применим K, а потом U^T, мы получим:
  U^T(K(C)) = U^T(F(C), β_C) = F(C)
3. Следовательно: U^T ∘ K = F.

▌Глубокий смысл:
Функтор K говорит нам: «Даже если у F нет левого сопряженного в исходной категории (𝒞), мы можем отобразить (𝒞) в новую категорию (алгебр), где F начнет выглядеть как часть сопряжения».

Это и есть реализация идеи «виртуального сопряжения»:
•  Мы берем F.
•  Строим по нему монаду коплотности T.
•  Строим категорию алгебр (𝒟)^T.
•  С помощью K «вкладываем» нашу исходную категорию (𝒞) в (𝒟)^T.

В этой новой среде объекты из (𝒞) (через K) становятся полноценными алгебрами, и мы можем работать с ними, используя весь арсенал теории монад, даже если изначально F не обладал никакими хорошими свойствами сопряжения.

Резюме: Чтобы получить K, просто используйте F для объектов и компоненты Кан-коединицы β для структур алгебр. Это самый канонический путь из возможных.



# Свободный функтор
Свободный функтор $F^T: (𝒟)→(𝒟)^T$ — это стандартная «половина» сопряжения, которое порождает любую монаду. В случае монады коплотности $T = Ran_F F$, этот функтор обладает рядом очень интересных интерпретаций.

Вот всё, что нам о нём известно:

▌1. Определение (стандартное для монад)
Для любой монады $(T, η, μ)$ свободный функтор $F^T$ определяется так:
•   На объектах: Он сопоставляет объекту D ∈(𝒟) свободную алгебру на этом объекте.
    $F^T(D) = (T(D), μ_D)$
    Здесь носителем алгебры является результат действия монады $T(D)$, а структурой алгебры — умножение монады $μ_D$.
•   На морфизмах: Для $f: D → D'$ морфизм $F^T(f)$ — это просто $T(f)$, который автоматически является морфизмом алгебр.

▌2. В контексте монады коплотности $(T = Ran_F F)$
Поскольку здесь $T(D) = ∫_C [(𝒟)(D, FC), FC]$, свободный объект $F^T(D)$ — это не просто абстрактная структура, а алгебра «всевозможных наблюдений» за объектом D через призму функтора F.

•   Объект $T(D)$ содержит в себе все способы трансформировать «входящие» в D стрелки в «исходящие» из образов F стрелки.
•   Свободный функтор $F^T$ берет «голый» объект D и «одевает» его во все возможные контексты, которые может предоставить функтор F.

▌3. Главное свойство: Каноническое сопряжение
Для монады коплотности именно пара функторов $(F^T, U^T)$ образует то самое реальное сопряжение, о котором мы говорили ранее:
$F^T ⊣ U^T$
Это сопряжение существует всегда, даже если у исходного функтора $F: (𝒞)→(𝒟)$ нет левого сопряженного. 

Почему это важно:
Милевски говорит, что каждая монада — коплотная. Это значит, что если вы возьмете любую монаду T, вы можете забыть, откуда она взялась, построить $F^T ⊣ U^T$, и тогда T окажется в точности $Ran_{U^T} U^T$.

▌4. Связь с функтором сравнения K
У нас есть два способа попасть в категорию алгебр (𝒟)^T:
1.  Из базовой категории (𝒟) через свободный функтор: F^T: (𝒟)→(𝒟)^T.
2.  Из исходной категории (𝒞) через функтор сравнения: K: (𝒞)→(𝒟)^T.

Их связывает интересное отношение. Если бы у F был левый сопряженный L, то была бы верна формула:
$K ≅ F^T ∘ L$
Но так как у нас L может не быть, K выступает как самостоятельный «первопроходец». Однако, если мы посмотрим на это через призму универсальных свойств, то F^T — это способ построить наилучшую аппроксимацию левого сопряженного к U^T.

▌5. Интуиция для программиста (Scala/Haskell)
Если мы используем монаду коплотности (например, Codensity в Haskell), то свободный функтор F^T — это то, что делает функция lift или return для этой монады.

•   Когда вы вызываете return x в монаде Codensity, вы по сути применяете свободный функтор F^T.
•   Вы превращаете обычное значение типа D в «свободную структуру» (алгебру), которая умеет взаимодействовать с любыми продолжениями.

▌Резюме:
Свободный функтор F^T:
1.  Создает структуру: Он превращает объект D в объект TD и наделяет его правилом схлопывания μ_D.
2.  Замыкает сопряжение: Он является левым сопряженным к забывающему функтору U^T, тем самым оправдывая существование монады T.
3.  Обобщает сопряжение: Если сопряжение L ⊣ F существовало, F^T просто «подхватывает» его работу. Если нет — он создает новую категорию «идеальных» объектов (алгебр), где сопряжение начинает работать.

Если U^T — это способ «вернуться в реальный мир» из мира монад, то F^T — это единственный законный способ «войти» в мир монад из реального мира.
